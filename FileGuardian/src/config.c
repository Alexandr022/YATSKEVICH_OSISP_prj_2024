#include "config.h"
#include <stdio.h>

void load_configuration(const char* filename) {
    // Реализация загрузки конфигурационного файла
    printf("Загружаем конфигурацию из файла: %s\n", filename);
}

int get_interval() {
    // Здесь должна быть реализация получения интервала из конфигурации
    return 10; // Временная заглушка, возвращаем 10 секунд
}
